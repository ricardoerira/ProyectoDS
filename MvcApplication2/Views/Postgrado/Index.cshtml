@model IEnumerable<MvcApplication2.Models.Postgrado>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipo_postgrado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.departamento_saludId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_postgrado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.departamento_saludId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.postgradoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.postgradoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.postgradoId })
        </td>
    </tr>
}

</table>
