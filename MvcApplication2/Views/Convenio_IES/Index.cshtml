@model IEnumerable<MvcApplication2.Models.Convenio_IES>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.objeto_convenio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IESId1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IESId2)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.objeto_convenio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IESId1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IESId2)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Convenio_IESID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Convenio_IESID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Convenio_IESID })
        </td>
    </tr>
}

</table>
